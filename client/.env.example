# ── Client .env ────────────────────────────────────────────────────────────────
# Copy to .env and fill in your values. Never commit the real .env.

# Backend API URL — must end with /api
# macOS: use 5001 (5000 is reserved for AirPlay)
VITE_API_URL=http://localhost:5001/api
